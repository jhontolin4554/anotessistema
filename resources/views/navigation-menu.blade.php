
                            <!-- Authentication -->
                            <form method="POST" action="{{ route('logout') }}" x-data>
                                @csrf

                                 href="{{ route('logout') }}"

                                    {{ __('Log Out') }}

                            </form>
